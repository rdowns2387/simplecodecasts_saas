<div class="home-page text-center">
    <img class="img-responsive center-block" src="/system/img/devmatch.png" style="width:350px">
    <h1>Welcome to Dev Match</h1>
    <h3>A membership site where entrepeneurs can meet developers.</h3>
</div>
<div class="text-center">
    <h1 class="join-header">JOIN THE COMMUNITY</h1>
</div>
<div class="row">
    <% if user_signed_in? %>
        <div class="col-md-3 col-md-offset-3">
            <div class="well">
                <h2 class="text-center">Your Profile</h2>
                <% if current_user.profile %>
                    <%= link_to "View your Profile", user_path(current_user), class: "btn btn-primary btn-block" %>
                    <%= link_to "Edit your Profile", edit_user_profile_path(current_user), class: "btn btn-primary btn-block" %>
                <% else %>
                    <p class="text-center">Create your profile to share with the community!</p>
                    <%= link_to "Create your profile", new_user_profile_path(current_user), class: "btn btn-primary btn-block" %>
                <% end %>
            </div>
        </div>
        <div class="col-md-3">
            <div class="well">
                <h2 class="text-center"> The Community</h2>
                <p class="text-center">Visit the community to meet developers, entrepreneurs, and investors!</p>
                <%= link_to "Visit the community", users_path, class: "btn btn-primary btn-block" %>
            </div>
        </div>
    <% else %>
        <div class="col-md-3 col-md-offset-3">
            <div class="well membership text-center">
                <h2 class="jumbotron basic-membership">Basic</h2>
                <h1> $0</h1>
                <p>FOREVER</p>
                <h4>Be <b>discovered</b> by Developers, Entrepreneurs, and Investors</h4>
                <h4><b>Limited access</b> the Dev Match Community</h4>
                <%= link_to "Sign Up", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-lg' %>
            </div>
        </div>
        <div class="col-md-3">
            <div class="well membership text-center">
                <h2 class="jumbotron pro-membership">Pro</h2>
                <h1>$10</h1>
                <p>MONTHLY</p>
                <h4>Seek out the perfect partner-in-crime for your next great idea</h4>
                <h4><b>Unlimited access</b> to Dev Match Community contact information</h4>
                <%= link_to "Sign Up", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-primary btn-lg' %>
            </div>
        </div>
    <% end %>
</div>